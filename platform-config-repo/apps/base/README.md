# apps/base/
This folder holds SHARED Application templates used as a base
for environment-specific overlays (dev/staging/prod).
In a simpler setup, you can leave this empty and put all
Application YAMLs directly in apps/dev, apps/staging, apps/prod.
